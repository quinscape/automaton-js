import React from "react"

const TOCSidebar = ({children}) => {

    return (
        <div className="mt-5 sidebar">
        {
            children
        }
        </div>
    );
};

export default TOCSidebar;
