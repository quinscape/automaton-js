import React from "react"
import cx from "classnames"
import { observer } from "mobx-react-lite";


const ValuesInput = observer(({}) => {

    return (
        <div>Values</div>
    );
});

export default ValuesInput;
