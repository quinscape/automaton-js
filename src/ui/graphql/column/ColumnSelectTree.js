import React, {useState} from "react";

const ColumnSelectTree = (props) => {
    const {
        rootElement,
        columns,
        relations,
        multiSelect
    } = props;

    const {selectedElements, setSelectedElements} = useState([]);

    //"SELECT * FROM Table1 AS "fdshfbds..gfds.gfds.gdsf.g", Table2 where Table1.banane = Table2.apfel"
}

export default ColumnSelectTree;
